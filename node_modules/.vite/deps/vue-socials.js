import "./chunk-XZR4NNMS.js";
import {
  defineComponent,
  h,
  markRaw
} from "./chunk-K7YJMR53.js";
import {
  __export
} from "./chunk-OWFZW76L.js";

// node_modules/vue-socials/dist/esm/components/index.js
var components_exports = {};
__export(components_exports, {
  SBlogger: () => SBlogger_default,
  SDevTo: () => SDevTo_default,
  SDiaspora: () => SDiaspora_default,
  SDouban: () => SDouban_default,
  SEmail: () => SEmail_default,
  SEvernote: () => SEvernote_default,
  SFacebook: () => SFacebook_default,
  SFacebookCount: () => SFacebookCount_default,
  SFacebookMessenger: () => SFacebookMessenger_default,
  SFacebookWorkplace: () => SFacebookWorkplace_default,
  SFlipBoard: () => SFlipBoard_default,
  SGithub: () => SGithub_default,
  SGithubCount: () => SGithubCount_default,
  SGithubGist: () => SGithubGist_default,
  SGithubGistCount: () => SGithubGistCount_default,
  SGithubRepo: () => SGithubRepo_default,
  SGithubRepoCount: () => SGithubRepoCount_default,
  SGmail: () => SGmail_default,
  SGoogleBookmarks: () => SGoogleBookmarks_default,
  SHackerNews: () => SHackerNews_default,
  SHatena: () => SHatena_default,
  SHatenaCount: () => SHatenaCount_default,
  SHouzz: () => SHouzz_default,
  SInstaPaper: () => SInstaPaper_default,
  SKakaoStory: () => SKakaoStory_default,
  SLine: () => SLine_default,
  SLineCount: () => SLineCount_default,
  SLinkedIn: () => SLinkedIn_default,
  SLiveJournal: () => SLiveJournal_default,
  SMailRu: () => SMailRu_default,
  SMastodon: () => SMastodon_default,
  SMicrosoftTeams: () => SMicrosoftTeams_default,
  SOdnoklassniki: () => SOdnoklassniki_default,
  SOdnoklassnikiCount: () => SOdnoklassnikiCount_default,
  SPinterest: () => SPinterest_default,
  SPinterestCount: () => SPinterestCount_default,
  SPocket: () => SPocket_default,
  SQZone: () => SQZone_default,
  SReddit: () => SReddit_default,
  SRedditCount: () => SRedditCount_default,
  SRenRen: () => SRenRen_default,
  SSkype: () => SSkype_default,
  SSms: () => SSms_default,
  STelegram: () => STelegram_default,
  SThreema: () => SThreema_default,
  STrello: () => STrello_default,
  STumblr: () => STumblr_default,
  STumblrCount: () => STumblrCount_default,
  STwitter: () => STwitter_default,
  SViber: () => SViber_default,
  SVkontakte: () => SVkontakte_default,
  SVkontakteCount: () => SVkontakteCount_default,
  SWeibo: () => SWeibo_default,
  SWhatsApp: () => SWhatsApp_default,
  SWordPress: () => SWordPress_default,
  SXing: () => SXing_default,
  SYahoo: () => SYahoo_default,
  SYammer: () => SYammer_default
});

// node_modules/vue-socials/dist/esm/utils/getFormattedWindowFeatures.js
function getFormattedWindowFeatures(object) {
  var keys = Object.keys(object);
  return keys.map(function(key) {
    return "".concat(key, "=").concat(Number(object[key]));
  }).join(",");
}
var getFormattedWindowFeatures_default = getFormattedWindowFeatures;

// node_modules/vue-socials/dist/esm/utils/getPopupClientRect.js
function getPopupClientRect(width, height) {
  var systemZoom = window.innerWidth / window.screen.availWidth;
  var finalWidth = width / systemZoom;
  var finalHeight = height / systemZoom;
  var left = (window.innerWidth - width) / 2 / systemZoom + window.screenLeft;
  var top = (window.innerHeight - height) / 2 / systemZoom + window.screenTop;
  return {
    width: finalWidth,
    height: finalHeight,
    top,
    left
  };
}
var getPopupClientRect_default = getPopupClientRect;

// node_modules/vue-socials/dist/esm/utils/inspect.js
function isArray(value) {
  return Array.isArray(value);
}
function isUndefined(value) {
  return value === void 0;
}

// node_modules/vue-socials/dist/esm/mixins/BaseSocial/BaseSocial.js
var DEFAULT_WINDOW_FEATURES = {
  width: 600,
  height: 540
};
function BaseSocials(name, customWindowFeatures, customShareOptions, customAriaLabel, isShareOptionsRequired, isWindowFeaturesRequired) {
  return defineComponent({
    props: {
      /**
       * Requested features of the new window
       * @link https://developer.mozilla.org/en-US/docs/Web/API/Window/open#Window_features
       */
      windowFeatures: {
        type: Object,
        default: function _default() {
          return customWindowFeatures || DEFAULT_WINDOW_FEATURES;
        },
        required: isWindowFeaturesRequired
      },
      /**
       * Share parameters for social network
       */
      shareOptions: {
        type: Object,
        default: function _default() {
          return customShareOptions || {};
        },
        required: isShareOptionsRequired || true
      },
      /**
       * Use native link behavior instead of window.open()
       */
      useNativeBehavior: {
        type: Boolean,
        default: false
      }
    },
    data: function data() {
      return {
        dialog: markRaw({
          shareDialog: null,
          shareDialogCloseIntervalId: void 0
        })
      };
    },
    emits: ["click", "popup-block", "popup-open", "popup-close", "popup-focus"],
    /**
     * Make sure interval has been cleared
     */
    beforeUnmount: function beforeUnmount() {
      window.clearInterval(this.dialog.shareDialogCloseIntervalId);
    },
    computed: {
      /**
       * Merge default and user window features
       */
      mergedWindowFeatures: function mergedWindowFeatures() {
        var windowFeatures = this.windowFeatures;
        return Object.assign({}, DEFAULT_WINDOW_FEATURES, windowFeatures);
      },
      /**
       * Calculate the aria-label for a link.
       * It replaces @s in a string with a social network name.
       */
      ariaLabel: function ariaLabel5() {
        var $attrs = this.$attrs;
        var target = $attrs.target;
        var label = customAriaLabel || "Share this with @s.";
        if (target === "_blank" || isUndefined(target)) {
          label += " (opens in new window)";
        }
        return label.replace(/@s/g, name);
      }
    },
    methods: {
      /**
       * Create new share popup from url
       * @link https://developer.mozilla.org/en-US/docs/Web/API/Window/open#Syntax
       */
      openShareDialog: function openShareDialog(url) {
        var _this$dialog$shareDia, _this = this;
        var mergedWindowFeatures = this.mergedWindowFeatures;
        var _mergedWindowFeatures = mergedWindowFeatures.width, width = _mergedWindowFeatures === void 0 ? DEFAULT_WINDOW_FEATURES.width : _mergedWindowFeatures, _mergedWindowFeatures2 = mergedWindowFeatures.height, height = _mergedWindowFeatures2 === void 0 ? DEFAULT_WINDOW_FEATURES.height : _mergedWindowFeatures2;
        var shareDialogClientRect = getPopupClientRect_default(width, height);
        var formattedFeatures = getFormattedWindowFeatures_default(Object.assign({}, mergedWindowFeatures, shareDialogClientRect));
        if (this.dialog.shareDialog === null || (_this$dialog$shareDia = this.dialog.shareDialog) !== null && _this$dialog$shareDia !== void 0 && _this$dialog$shareDia.closed) {
          this.dialog.shareDialog = window.open(url, "_blank", formattedFeatures);
          if (!this.dialog.shareDialog) {
            this.$emit("popup-block");
            return;
          }
          this.$emit("popup-open");
          this.dialog.shareDialogCloseIntervalId = window.setInterval(function() {
            var _this$dialog$shareDia2;
            if (_this.dialog.shareDialog === null || (_this$dialog$shareDia2 = _this.dialog.shareDialog) !== null && _this$dialog$shareDia2 !== void 0 && _this$dialog$shareDia2.closed) {
              window.clearInterval(_this.dialog.shareDialogCloseIntervalId);
              _this.$emit("popup-close");
              _this.dialog.shareDialog = null;
            }
          }, 300);
        } else {
          this.dialog.shareDialog.focus();
          this.$emit("popup-focus");
        }
      },
      /**
       * Create new share component
       */
      generateComponent: function generateComponent(url) {
        var _this2 = this, _this$$slots$default, _this$$slots;
        return h("a", {
          href: url,
          target: "_blank",
          rel: "nofollow noopener noreferrer",
          "aria-label": this.ariaLabel,
          onClick: function onClick(event) {
            if (!_this2.useNativeBehavior) {
              event.preventDefault();
              _this2.openShareDialog(url);
            }
            _this2.$emit("click");
          }
        }, (_this$$slots$default = (_this$$slots = this.$slots).default) === null || _this$$slots$default === void 0 ? void 0 : _this$$slots$default.call(_this$$slots));
      }
    }
  });
}
var BaseSocial_default = BaseSocials;

// node_modules/vue-socials/dist/esm/utils/getSerialisedParams.js
function getSerialisedParams(object) {
  var params = Object.entries(object).filter(function(param) {
    return param[1] !== void 0 && param[1] !== null && !Number.isNaN(param[1]) && param[1] !== "";
  }).map(function(param) {
    return "".concat(encodeURIComponent(param[0]), "=").concat(encodeURIComponent(String(param[1])));
  });
  return params.length > 0 ? "?".concat(params.join("&")) : "";
}
var getSerialisedParams_default = getSerialisedParams;

// node_modules/vue-socials/dist/esm/components/SBlogger/SBlogger.js
var SBlogger = defineComponent({
  name: "SBlogger",
  mixins: [BaseSocial_default("Blogger", void 0, void 0, void 0, true)],
  computed: {
    networkURL: function networkURL() {
      var BASE_URL = "https://www.blogger.com/blog-this.g";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        u: url,
        t: text,
        n: title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render() {
    return this.generateComponent(this.networkURL);
  }
});
var SBlogger_default = SBlogger;

// node_modules/vue-socials/dist/esm/utils/getSeparatedList.js
function getSeparatedList(array) {
  var separator = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : ",";
  if (isArray(array)) {
    return array.join(separator);
  }
  return "";
}
var getSeparatedList_default = getSeparatedList;

// node_modules/vue-socials/dist/esm/components/SDevTo/SDevTo.js
var SDevTo = defineComponent({
  name: "SDevTo",
  mixins: [BaseSocial_default("DevTo")],
  computed: {
    networkURL: function networkURL2() {
      var BASE_URL = "https://dev.to/new";
      var shareOptions = this.shareOptions;
      var title = shareOptions.title, isPublished = shareOptions.isPublished, tags = shareOptions.tags, content = shareOptions.content;
      var formattedTitle = title ? "title:".concat(title) : "";
      var formattedPublish = isPublished ? "published:".concat(isPublished) : "";
      var formattedTags = tags ? "tags:".concat(getSeparatedList_default(tags, ", ")) : "";
      var formattedContent = content || "";
      var serialisedParams = getSerialisedParams_default({
        prefill: "\n        ---\n        ".concat(formattedTitle, "\n        ").concat(formattedPublish, "\n        ").concat(formattedTags, "\n        ---\n        ").concat(formattedContent)
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render2() {
    return this.generateComponent(this.networkURL);
  }
});
var SDevTo_default = SDevTo;

// node_modules/vue-socials/dist/esm/components/SDiaspora/SDiaspora.js
var SDiaspora = defineComponent({
  name: "SDiaspora",
  mixins: [BaseSocial_default("Diaspora", {
    width: 600,
    height: 540
  })],
  computed: {
    networkURL: function networkURL3() {
      var BASE_URL = "https://share.diasporafoundation.org";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        url,
        title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render3() {
    return this.generateComponent(this.networkURL);
  }
});
var SDiaspora_default = SDiaspora;

// node_modules/vue-socials/dist/esm/components/SDouban/SDouban.js
var SDouban = defineComponent({
  name: "SDouban",
  mixins: [BaseSocial_default("Douban", {
    width: 1100,
    height: 450
  })],
  computed: {
    networkURL: function networkURL4() {
      var BASE_URL = "http://www.douban.com/recommend/";
      var shareOptions = this.shareOptions;
      var title = shareOptions.title, url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        url,
        title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render4() {
    return this.generateComponent(this.networkURL);
  }
});
var SDouban_default = SDouban;

// node_modules/vue-socials/dist/esm/components/SEmail/SEmail.js
var SEmail = defineComponent({
  name: "SEmail",
  props: {
    /**
     * Share parameters for Email
     */
    shareOptions: {
      type: Object,
      required: true
    }
  },
  computed: {
    networkURL: function networkURL5() {
      var BASE_URL = "mailto:";
      var shareOptions = this.shareOptions;
      var mail = shareOptions.mail, subject = shareOptions.subject, body = shareOptions.body, cc = shareOptions.cc, bcc = shareOptions.bcc;
      var serialisedParams = getSerialisedParams_default({
        subject,
        body,
        cc: getSeparatedList_default(cc, ", "),
        bcc: getSeparatedList_default(bcc, ", ")
      });
      return "".concat(BASE_URL).concat(mail).concat(serialisedParams);
    },
    ariaLabel: function ariaLabel() {
      var shareOptions = this.shareOptions;
      var mail = shareOptions.mail, cc = shareOptions.cc, bcc = shareOptions.bcc;
      var LABELS = ["Send an e-mail to ".concat(mail)];
      if (cc) {
        LABELS.push("add a carbon copy for ".concat(getSeparatedList_default(cc, ", ")));
      }
      if (bcc) {
        LABELS.push("add a blind carbon copy for ".concat(getSeparatedList_default(bcc, ", ")));
      }
      return "".concat(LABELS.join(" and "), ".");
    }
  },
  render: function render5() {
    var _this$$slots$default, _this$$slots;
    return h("a", {
      href: this.networkURL,
      rel: "noopener noreferrer",
      "aria-label": this.ariaLabel
    }, (_this$$slots$default = (_this$$slots = this.$slots).default) === null || _this$$slots$default === void 0 ? void 0 : _this$$slots$default.call(_this$$slots));
  }
});
var SEmail_default = SEmail;

// node_modules/vue-socials/dist/esm/components/SEvernote/SEvernote.js
var SEvernote = defineComponent({
  name: "SEvernote",
  mixins: [BaseSocial_default("Evernote", {
    width: 800,
    height: 560
  })],
  computed: {
    networkURL: function networkURL6() {
      var BASE_URL = "https://www.evernote.com/clip.action";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        url,
        title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render6() {
    return this.generateComponent(this.networkURL);
  }
});
var SEvernote_default = SEvernote;

// node_modules/vue-socials/dist/esm/components/SFacebook/SFacebook.js
var SFacebook = defineComponent({
  name: "SFacebook",
  mixins: [BaseSocial_default("Facebook", {
    width: 685,
    height: 600
  })],
  computed: {
    networkURL: function networkURL7() {
      var BASE_URL = "https://www.facebook.com/sharer/sharer.php";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, quote = shareOptions.quote, hashtag = shareOptions.hashtag;
      return "".concat(BASE_URL).concat(getSerialisedParams_default({
        u: url,
        quote,
        hashtag
      }));
    }
  },
  render: function render7() {
    return this.generateComponent(this.networkURL);
  }
});
var SFacebook_default = SFacebook;

// node_modules/vue-socials/dist/esm/utils/jsonp.js
function JSONP(url, callback, callbackName) {
  var script = document.createElement("script");
  var cbName = callbackName || "callback";
  if (callback) {
    if (!window.callbackRegistry) {
      window.callbackRegistry = {};
    }
    var _key = "cb".concat(String(Math.random()).slice(-6));
    script.src = "".concat(url).concat(url.indexOf("?") === -1 ? "?" : "&").concat(cbName, "=callbackRegistry.").concat(_key);
    script.onerror = function JSONPOnError() {
      delete window.callbackRegistry[_key];
      callback(new Error(), null);
    };
    window.callbackRegistry[_key] = function JSONPCallback(response) {
      delete window.callbackRegistry[_key];
      callback(null, response);
    };
  } else {
    script.src = url;
  }
  document.head.removeChild(document.head.appendChild(script));
}
var jsonp_default = JSONP;

// node_modules/vue-socials/dist/esm/mixins/BaseCount/BaseCount.js
function BaseCount(name, customShareOptions, isShareOptionsRequired, customAriaLabel) {
  return defineComponent({
    props: {
      /**
       * Component tag
       */
      tag: {
        type: [String, Object],
        default: "span"
      },
      /**
       * Share parameters for social network
       */
      shareOptions: {
        type: Object,
        default: function _default() {
          return customShareOptions || {};
        },
        required: isShareOptionsRequired || true
      }
    },
    data: function data() {
      return {
        count: void 0,
        response: null,
        error: null,
        isLoading: false
      };
    },
    emits: ["load", "error", "loading"],
    computed: {
      /**
       * Calculate the aria-label for a counter.
       * It replaces @s in a string with a social network name
       * and @c with a count.
       */
      ariaLabel: function ariaLabel5() {
        var count = this.count;
        var label = customAriaLabel || "@c people share this on @s.";
        if (!isUndefined(count)) {
          return label.replace(/@c/g, String(count)).replace(/@s/g, name);
        }
        return "No one shares this content yet.";
      }
    },
    methods: {
      /**
       * Save response from JSONP or HTTP and emit event
       */
      handleResult: function handleResult(value) {
        this.response = value;
        this.$emit("load", value);
      },
      /**
       * Save response from JSONP or HTTP and emit event
       */
      handleError: function handleError(value) {
        this.error = value;
        this.$emit("error", value);
      },
      /**
       * Save loading state and emit event
       */
      handleLoading: function handleLoading(value) {
        this.isLoading = value;
        this.$emit("loading", value);
      },
      /**
       * Save counter value and render inside element
       */
      handleCount: function handleCount(count) {
        this.count = count;
      },
      /**
       * Create new count component
       */
      generateComponent: function generateComponent() {
        var _this$$slots$default, _this$$slots;
        var children = ((_this$$slots$default = (_this$$slots = this.$slots).default) === null || _this$$slots$default === void 0 ? void 0 : _this$$slots$default.call(_this$$slots, {
          isLoading: this.isLoading,
          response: this.response,
          count: this.count
        })) || [this.count];
        return h(this.tag, {
          "aria-label": this.ariaLabel
        }, children);
      }
    },
    render: function render48() {
      return this.generateComponent();
    }
  });
}
var BaseCount_default = BaseCount;

// node_modules/vue-socials/dist/esm/components/SFacebookCount/SFacebookCount.js
var SFacebookCount = defineComponent({
  mixins: [BaseCount_default("Facebook")],
  methods: {
    handleFacebookResponse: function handleFacebookResponse(data) {
      this.handleResult(data);
      if ("engagement" in data) {
        var _data$engagement;
        this.handleCount((_data$engagement = data.engagement) === null || _data$engagement === void 0 ? void 0 : _data$engagement.share_count);
      }
    }
  },
  mounted: function mounted() {
    var _this = this;
    var BASE_URL = "https://graph.facebook.com/";
    var shareOptions = this.shareOptions;
    var id = shareOptions.id, accessToken = shareOptions.accessToken, fields = shareOptions.fields, scopes = shareOptions.scopes;
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      id,
      access_token: accessToken,
      fields: getSeparatedList_default(fields),
      scopes: getSeparatedList_default(scopes)
    }));
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (data) {
        _this.handleFacebookResponse(data);
      }
      if (err) {
        _this.handleError(err);
      }
    });
  }
});
var SFacebookCount_default = SFacebookCount;

// node_modules/vue-socials/dist/esm/components/SFacebookMessenger/SFacebookMessenger.js
var SFacebookMessenger = defineComponent({
  name: "SFacebookMessenger",
  mixins: [BaseSocial_default("Facebook Messenger")],
  computed: {
    networkURL: function networkURL8() {
      var BASE_URL = "http://www.facebook.com/dialog/send";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, appId = shareOptions.appId, redirectUri = shareOptions.redirectUri, to = shareOptions.to;
      var serialisedParams = getSerialisedParams_default({
        link: url,
        app_id: appId,
        redirect_uri: redirectUri,
        to
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render8() {
    return this.generateComponent(this.networkURL);
  }
});
var SFacebookMessenger_default = SFacebookMessenger;

// node_modules/vue-socials/dist/esm/components/SFacebookWorkplace/SFacebookWorkplace.js
var SFacebookWorkplace = defineComponent({
  name: "SFacebookWorkplace",
  mixins: [BaseSocial_default("Facebook Workplace")],
  computed: {
    networkURL: function networkURL9() {
      var BASE_URL = "https://work.facebook.com/sharer.php";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        url
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render9() {
    return this.generateComponent(this.networkURL);
  }
});
var SFacebookWorkplace_default = SFacebookWorkplace;

// node_modules/vue-socials/dist/esm/components/SFlipBoard/SFlipBoard.js
var SFlipBoard = defineComponent({
  name: "SFlipBoard",
  mixins: [BaseSocial_default("Flipboard", {
    width: 834,
    height: 572
  })],
  computed: {
    networkURL: function networkURL10() {
      var BASE_URL = "https://share.flipboard.com/bookmarklet/popout";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        v: "2",
        url,
        title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render10() {
    return this.generateComponent(this.networkURL);
  }
});
var SFlipBoard_default = SFlipBoard;

// node_modules/vue-socials/dist/esm/components/SGithub/SGithub.js
var GITHUB_LINK_TYPES = {
  sponsor: "sponsor",
  follow: "follow",
  profile: "profile"
};
var SGithub = defineComponent({
  name: "SGithub",
  mixins: [BaseSocial_default("GitHub", {
    width: 900,
    height: 600
  })],
  computed: {
    networkURL: function networkURL11() {
      var BASE_URL = "https://github.com/";
      var shareOptions = this.shareOptions;
      var username = shareOptions.username, type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES.sponsor:
          return "".concat(BASE_URL, "sponsors/").concat(username);
        case GITHUB_LINK_TYPES.follow:
          return "".concat(BASE_URL).concat(username, "?tab=followers");
        case GITHUB_LINK_TYPES.profile:
        default:
          return "".concat(BASE_URL).concat(username);
      }
    },
    ariaLabel: function ariaLabel2() {
      var shareOptions = this.shareOptions;
      var username = shareOptions.username, type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES.sponsor:
          return "Sponsor ".concat(username, " on GitHub.");
        case GITHUB_LINK_TYPES.follow:
          return "Follow ".concat(username, " on GitHub.");
        case GITHUB_LINK_TYPES.profile:
        default:
          return "Watch ".concat(username, " on GitHub.");
      }
    }
  },
  render: function render11() {
    return this.generateComponent(this.networkURL);
  }
});
var SGithub_default = SGithub;

// node_modules/vue-socials/dist/esm/components/SGithubCount/SGithubCount.js
var GITHUB_LINK_TYPES2 = {
  follow: "follow"
};
var SGithubCount = defineComponent({
  name: "SGithubCount",
  mixins: [BaseCount_default("GitHub", {}, true, "@c followers on @s.")],
  methods: {
    handleGithubResponse: function handleGithubResponse(data) {
      this.handleResult(data);
      if ("followers" in data.data) {
        this.handleCount(data.data.followers);
      }
    }
  },
  mounted: function mounted2() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var username = shareOptions.username, type = shareOptions.type;
    var BASE_URL = "https://api.github.com/";
    var finalURL;
    switch (type) {
      case GITHUB_LINK_TYPES2.follow:
      default:
        finalURL = "".concat(BASE_URL, "users/").concat(username);
    }
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (data) {
        _this.handleGithubResponse(data);
      }
      if (err) {
        _this.handleError(err);
      }
    });
  }
});
var SGithubCount_default = SGithubCount;

// node_modules/vue-socials/dist/esm/components/SGithubGist/SGithubGist.js
var GITHUB_LINK_TYPES3 = {
  gist: "gist",
  star: "star",
  fork: "fork",
  download: "download"
};
var SGithubGist = defineComponent({
  name: "SGithubGist",
  mixins: [BaseSocial_default("GitHub Gists", {
    width: 900,
    height: 600
  })],
  computed: {
    networkURL: function networkURL12() {
      var BASE_URL = "https://gist.github.com/";
      var shareOptions = this.shareOptions;
      var username = shareOptions.username, gistId = shareOptions.gistId, type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES3.fork:
          return "".concat(BASE_URL).concat(username, "/").concat(gistId, "/forks");
        case GITHUB_LINK_TYPES3.star:
          return "".concat(BASE_URL).concat(username, "/").concat(gistId, "/stargazers");
        case GITHUB_LINK_TYPES3.download:
          return "".concat(BASE_URL).concat(username, "/").concat(gistId, "/archive/master.zip");
        case GITHUB_LINK_TYPES3.gist:
        default:
          return "".concat(BASE_URL).concat(username, "/").concat(gistId);
      }
    },
    ariaLabel: function ariaLabel3() {
      var shareOptions = this.shareOptions;
      var type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES3.fork:
          return "Fork this gist.";
        case GITHUB_LINK_TYPES3.star:
          return "Star this gist.";
        case GITHUB_LINK_TYPES3.download:
          return "Download this gist.";
        case GITHUB_LINK_TYPES3.gist:
        default:
          return "Watch this gist.";
      }
    }
  },
  render: function render12() {
    return this.generateComponent(this.networkURL);
  }
});
var SGithubGist_default = SGithubGist;

// node_modules/vue-socials/dist/esm/components/SGithubGistCount/SGithubGistCount.js
var GITHUB_LINK_TYPES4 = {
  fork: "fork"
};
var SGithubGistCount = defineComponent({
  name: "SGithubGistCount",
  methods: {
    handleGithubResponse: function handleGithubResponse2(data) {
      var shareOptions = this.shareOptions;
      var type = shareOptions.type;
      this.handleResult(data);
      var count;
      switch (type) {
        default:
        case GITHUB_LINK_TYPES4.fork:
          if ("forks" in data.data) {
            count = data.data.forks.length;
          }
          break;
      }
      this.handleCount(count);
    }
  },
  mixins: [BaseCount_default("GithubGist")],
  mounted: function mounted3() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var gistId = shareOptions.gistId;
    var BASE_URL = "https://api.github.com/";
    var finalURL = "".concat(BASE_URL, "gists/").concat(gistId);
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleGithubResponse(data);
      }
    });
  }
});
var SGithubGistCount_default = SGithubGistCount;

// node_modules/vue-socials/dist/esm/components/SGithubRepo/SGithubRepo.js
var GITHUB_LINK_TYPES5 = {
  repo: "repo",
  watch: "watch",
  star: "star",
  fork: "fork",
  download: "download",
  issue: "issue",
  pr: "pr",
  template: "template"
};
var SGithubRepo = defineComponent({
  name: "SGithubRepo",
  mixins: [BaseSocial_default("GitHub", {
    width: 900,
    height: 600
  })],
  computed: {
    networkURL: function networkURL13() {
      var BASE_URL = "https://github.com/";
      var shareOptions = this.shareOptions;
      var username = shareOptions.username, repository = shareOptions.repository, type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES5.watch:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/subscription");
        case GITHUB_LINK_TYPES5.fork:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/fork");
        case GITHUB_LINK_TYPES5.star:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/stargazers");
        case GITHUB_LINK_TYPES5.issue:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/issues/new");
        case GITHUB_LINK_TYPES5.pr:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/compare");
        case GITHUB_LINK_TYPES5.download:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/archive/master.zip");
        case GITHUB_LINK_TYPES5.template:
          return "".concat(BASE_URL).concat(username, "/").concat(repository, "/generate");
        case GITHUB_LINK_TYPES5.repo:
        default:
          return "".concat(BASE_URL).concat(username, "/").concat(repository);
      }
    },
    ariaLabel: function ariaLabel4() {
      var shareOptions = this.shareOptions;
      var username = shareOptions.username, repository = shareOptions.repository, type = shareOptions.type;
      switch (type) {
        case GITHUB_LINK_TYPES5.watch:
          return "Watch ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.fork:
          return "Fork ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.star:
          return "Give a star to ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.issue:
          return "Raise a new issue on ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.pr:
          return "Raise a new pull request ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.download:
          return "Download ".concat(username, "/").concat(repository, " from GitHub");
        case GITHUB_LINK_TYPES5.template:
          return "Generate a new repository from ".concat(username, "/").concat(repository, " on GitHub");
        case GITHUB_LINK_TYPES5.repo:
        default:
          return "Watch ".concat(username, "/").concat(repository, " on GitHub");
      }
    }
  },
  render: function render13() {
    return this.generateComponent(this.networkURL);
  }
});
var SGithubRepo_default = SGithubRepo;

// node_modules/vue-socials/dist/esm/components/SGithubRepoCount/SGithubRepoCount.js
var GITHUB_LINK_TYPES6 = {
  watch: "watch",
  star: "star",
  fork: "fork",
  issues: "issues"
};
var SGithubRepoCount = defineComponent({
  name: "SGithubRepoCount",
  mixins: [BaseCount_default("GitHub")],
  methods: {
    handleGithubResponse: function handleGithubResponse3(data) {
      var shareOptions = this.shareOptions;
      var type = shareOptions.type;
      this.handleResult(data);
      var count;
      switch (type) {
        case GITHUB_LINK_TYPES6.watch:
          if ("subscribers_count" in data.data) {
            count = data.data.subscribers_count;
          }
          break;
        case GITHUB_LINK_TYPES6.fork:
          if ("forks_count" in data.data) {
            count = data.data.forks_count;
          }
          break;
        case GITHUB_LINK_TYPES6.issues:
          if ("open_issues_count" in data.data) {
            count = data.data.open_issues_count;
          }
          break;
        case GITHUB_LINK_TYPES6.star:
        default:
          if ("stargazers_count" in data.data) {
            count = data.data.stargazers_count;
          }
          break;
      }
      this.handleCount(count);
    }
  },
  mounted: function mounted4() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var username = shareOptions.username, repository = shareOptions.repository;
    var BASE_URL = "https://api.github.com/";
    var finalURL = "".concat(BASE_URL, "repos/").concat(username, "/").concat(repository);
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleGithubResponse(data);
      }
    });
  }
});
var SGithubRepoCount_default = SGithubRepoCount;

// node_modules/vue-socials/dist/esm/components/SGmail/SGmail.js
var SGmail = defineComponent({
  name: "SGmail",
  mixins: [BaseSocial_default("Gmail")],
  computed: {
    networkURL: function networkURL14() {
      var BASE_URL = "https://mail.google.com/mail/";
      var shareOptions = this.shareOptions;
      var to = shareOptions.to, su = shareOptions.su, body = shareOptions.body, cc = shareOptions.cc, bcc = shareOptions.bcc;
      var serialisedParams = getSerialisedParams_default({
        view: "cm",
        to,
        su,
        body,
        cc,
        bcc
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render14() {
    return this.generateComponent(this.networkURL);
  }
});
var SGmail_default = SGmail;

// node_modules/vue-socials/dist/esm/components/SGoogleBookmarks/SGoogleBookmarks.js
var SGoogleBookmarks = defineComponent({
  name: "SGoogleBookmarks",
  mixins: [BaseSocial_default("Google Bookmarks")],
  computed: {
    networkURL: function networkURL15() {
      var BASE_URL = "https://www.google.com/bookmarks/mark";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, annotation = shareOptions.annotation, tags = shareOptions.tags;
      var serialisedParams = getSerialisedParams_default({
        op: "edit",
        output: "popup",
        bkmk: url,
        title,
        annotation,
        labels: getSeparatedList_default(tags)
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render15() {
    return this.generateComponent(this.networkURL);
  }
});
var SGoogleBookmarks_default = SGoogleBookmarks;

// node_modules/vue-socials/dist/esm/components/SHackerNews/SHackerNews.js
var SHackerNews = defineComponent({
  name: "SHackerNews",
  mixins: [BaseSocial_default("HackerNews")],
  computed: {
    networkURL: function networkURL16() {
      var BASE_URL = "https://news.ycombinator.com/submitlink";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        u: url,
        t: title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render16() {
    return this.generateComponent(this.networkURL);
  }
});
var SHackerNews_default = SHackerNews;

// node_modules/vue-socials/dist/esm/components/SHatena/SHatena.js
var SHatena = defineComponent({
  name: "SHatena",
  mixins: [BaseSocial_default("Hatena")],
  computed: {
    networkURL: function networkURL17() {
      var BASE_URL = "http://b.hatena.ne.jp/add";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        mode: "confirm",
        url,
        title
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render17() {
    return this.generateComponent(this.networkURL);
  }
});
var SHatena_default = SHatena;

// node_modules/vue-socials/dist/esm/components/SHatenaCount/SHatenaCount.js
var SHatenaCount = defineComponent({
  name: "SHatenaCount",
  mixins: [BaseCount_default("Hatena")],
  methods: {
    handleOKResponse: function handleOKResponse(count) {
      this.handleResult({
        count
      });
      this.handleCount(count);
    }
  },
  mounted: function mounted5() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "https://bookmark.hatenaapis.com/count/entry";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      url
    }));
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleOKResponse(data);
      }
    });
  }
});
var SHatenaCount_default = SHatenaCount;

// node_modules/vue-socials/dist/esm/components/SHouzz/SHouzz.js
var SHouzz = defineComponent({
  name: "SHouzz",
  mixins: [BaseSocial_default("Houzz", {
    width: 1100,
    height: 700
  })],
  computed: {
    networkURL: function networkURL18() {
      var BASE_URL = "https://www.houzz.com/imageClipperUpload";
      var shareOptions = this.shareOptions;
      var id = shareOptions.id, url = shareOptions.url, title = shareOptions.title, image = shareOptions.image, category = shareOptions.category;
      var serialisedParams = getSerialisedParams_default({
        link: url,
        hzid: id,
        imageUrl: image,
        title,
        category: getSeparatedList_default(category)
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render18() {
    return this.generateComponent(this.networkURL);
  }
});
var SHouzz_default = SHouzz;

// node_modules/vue-socials/dist/esm/components/SInstaPaper/SInstaPaper.js
var SInstaPaper = defineComponent({
  name: "SInstaPaper",
  mixins: [BaseSocial_default("InstaPaper", {
    width: 600,
    height: 600
  })],
  computed: {
    networkURL: function networkURL19() {
      var BASE_URL = "http://www.instapaper.com/edit";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, description = shareOptions.description;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        description
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render19() {
    return this.generateComponent(this.networkURL);
  }
});
var SInstaPaper_default = SInstaPaper;

// node_modules/vue-socials/dist/esm/components/SKakaoStory/SKakaoStory.js
var SKakaoStory = defineComponent({
  name: "SKakaoStory",
  mixins: [BaseSocial_default("KakaoStory")],
  computed: {
    networkURL: function networkURL20() {
      var BASE_URL = "https://story.kakao.com/s/share";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        url,
        text
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render20() {
    return this.generateComponent(this.networkURL);
  }
});
var SKakaoStory_default = SKakaoStory;

// node_modules/vue-socials/dist/esm/components/SLine/SLine.js
var SLine = defineComponent({
  name: "SLine",
  mixins: [BaseSocial_default("Line", {
    width: 600,
    height: 600
  })],
  computed: {
    networkURL: function networkURL21() {
      var BASE_URL = "https://lineit.line.me/share/ui";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        url,
        text
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render21() {
    return this.generateComponent(this.networkURL);
  }
});
var SLine_default = SLine;

// node_modules/vue-socials/dist/esm/utils/http.js
function HTTP(url, callback) {
  var request = new XMLHttpRequest();
  request.open("GET", url);
  request.onload = function onLoad() {
    if (request.readyState === 4 && request.status === 200) {
      callback(null, JSON.parse(request.response));
    }
  };
  request.onerror = function onError() {
    callback(request.statusText, null);
  };
  request.send();
}
var http_default = HTTP;

// node_modules/vue-socials/dist/esm/components/SLineCount/SLineCount.js
var SLineCount = defineComponent({
  name: "SLineCount",
  mixins: [BaseCount_default("Line")],
  methods: {
    handlePinterestResponse: function handlePinterestResponse(data) {
      this.handleResult(data);
      this.handleCount(data.share);
    }
  },
  mounted: function mounted6() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "https://api.line.me/social-plugin/metrics";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      url
    }));
    this.handleLoading(true);
    http_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handlePinterestResponse(data);
      }
    });
  }
});
var SLineCount_default = SLineCount;

// node_modules/vue-socials/dist/esm/components/SLinkedIn/SLinkedIn.js
var SLinkedIn = defineComponent({
  name: "SLinkedIn",
  mixins: [BaseSocial_default("LinkedIn")],
  computed: {
    networkURL: function networkURL22() {
      var BASE_URL = "https://www.linkedin.com/sharing/share-offsite/";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        url
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render22() {
    return this.generateComponent(this.networkURL);
  }
});
var SLinkedIn_default = SLinkedIn;

// node_modules/vue-socials/dist/esm/components/SLiveJournal/SLiveJournal.js
var SLiveJournal = defineComponent({
  name: "SLiveJournal",
  mixins: [BaseSocial_default("LiveJournal")],
  computed: {
    networkURL: function networkURL23() {
      var BASE_URL = "http://www.livejournal.com/update.bml";
      var shareOptions = this.shareOptions;
      var subject = shareOptions.subject, url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        event: url,
        subject
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render23() {
    return this.generateComponent(this.networkURL);
  }
});
var SLiveJournal_default = SLiveJournal;

// node_modules/vue-socials/dist/esm/components/SMailRu/SMailRu.js
var SMailRu = defineComponent({
  name: "SMailRu",
  mixins: [BaseSocial_default("Mail.ru")],
  computed: {
    networkURL: function networkURL24() {
      var BASE_URL = "https://connect.mail.ru/share";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, description = shareOptions.description, image = shareOptions.image, swfUrl = shareOptions.swfUrl, width = shareOptions.width, height = shareOptions.height;
      var serialisedParams = getSerialisedParams_default({
        share_url: url,
        title,
        description,
        image_url: image,
        swfurl: swfUrl,
        width,
        height
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render24() {
    return this.generateComponent(this.networkURL);
  }
});
var SMailRu_default = SMailRu;

// node_modules/vue-socials/dist/esm/components/SMastodon/SMastodon.js
var SMastodon = defineComponent({
  name: "SMastodon",
  mixins: [BaseSocial_default("Mastodon", {
    width: 600,
    height: 700
  })],
  computed: {
    networkURL: function networkURL25() {
      var shareOptions = this.shareOptions;
      var domain = shareOptions.domain, text = shareOptions.text, url = shareOptions.url;
      var BASE_URL = "".concat(domain, "/share");
      var serialisedParams = getSerialisedParams_default({
        text,
        url
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render25() {
    return this.generateComponent(this.networkURL);
  }
});
var SMastodon_default = SMastodon;

// node_modules/vue-socials/dist/esm/components/SMicrosoftTeams/SMicrosoftTeams.js
var SMicrosoftTeams = defineComponent({
  name: "SMicrosoftTeams",
  mixins: [BaseSocial_default("MicrosoftTeams")],
  computed: {
    networkURL: function networkURL26() {
      var BASE_URL = "https://teams.microsoft.com/share";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, preview = shareOptions.preview, text = shareOptions.text, instruction = shareOptions.instruction, title = shareOptions.title;
      var serialisedParams = getSerialisedParams_default({
        href: url,
        preview,
        assignTitle: title,
        msgText: text,
        assignInstr: instruction
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render26() {
    return this.generateComponent(this.networkURL);
  }
});
var SMicrosoftTeams_default = SMicrosoftTeams;

// node_modules/vue-socials/dist/esm/components/SOdnoklassniki/SOdnoklassniki.js
var SOdnoklassniki = defineComponent({
  name: "SOdnoklassniki",
  mixins: [BaseSocial_default("Odnoklassniki")],
  computed: {
    networkURL: function networkURL27() {
      var BASE_URL = "https://connect.ok.ru/offer";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, imageUrl = shareOptions.imageUrl;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        imageUrl
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render27() {
    return this.generateComponent(this.networkURL);
  }
});
var SOdnoklassniki_default = SOdnoklassniki;

// node_modules/vue-socials/dist/esm/components/SOdnoklassnikiCount/SOdnoklassnikiCount.js
var SOdnoklassnikiCount = defineComponent({
  name: "SOdnoklassnikiCount",
  mixins: [BaseCount_default("Odnoklassniki")],
  methods: {
    handleOKResponse: function handleOKResponse2(count) {
      this.handleResult({
        count
      });
      this.handleCount(count);
    }
  },
  mounted: function mounted7() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var ref = shareOptions.ref;
    var BASE_URL = "https://connect.ok.ru/dk";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      "st.cmd": "extLike",
      tp: "json",
      ref
    }));
    this.handleLoading(true);
    http_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleOKResponse(data.count);
      }
    });
  }
});
var SOdnoklassnikiCount_default = SOdnoklassnikiCount;

// node_modules/vue-socials/dist/esm/components/SPinterest/SPinterest.js
var SPinterest = defineComponent({
  name: "SPinterest",
  mixins: [BaseSocial_default("Pinterest", {
    width: 700,
    height: 700
  })],
  computed: {
    networkURL: function networkURL28() {
      var BASE_URL = "http://pinterest.com/pin/create/button/";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, media = shareOptions.media, description = shareOptions.description;
      var serialisedParams = getSerialisedParams_default({
        url,
        media,
        description
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render28() {
    return this.generateComponent(this.networkURL);
  }
});
var SPinterest_default = SPinterest;

// node_modules/vue-socials/dist/esm/components/SPinterestCount/SPinterestCount.js
var SPinterestCount = defineComponent({
  name: "SPinterestCount",
  mixins: [BaseCount_default("Pinterest")],
  methods: {
    handlePinterestResponse: function handlePinterestResponse2(data) {
      this.handleResult(data);
      this.handleCount(data.count);
    }
  },
  mounted: function mounted8() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "https://api.pinterest.com/v1/urls/count.json";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      url
    }));
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handlePinterestResponse(data);
      }
    });
  }
});
var SPinterestCount_default = SPinterestCount;

// node_modules/vue-socials/dist/esm/components/SPocket/SPocket.js
var SPocket = defineComponent({
  name: "SPocket",
  mixins: [BaseSocial_default("GetPocket")],
  computed: {
    networkURL: function networkURL29() {
      var BASE_URL = "https://getpocket.com/edit";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        url
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render29() {
    return this.generateComponent(this.networkURL);
  }
});
var SPocket_default = SPocket;

// node_modules/vue-socials/dist/esm/components/SQZone/SQZone.js
var SQZone = defineComponent({
  name: "SQZone",
  mixins: [BaseSocial_default("QZone")],
  computed: {
    networkURL: function networkURL30() {
      var BASE_URL = "http://sns.qzone.qq.com/cgi-bin/qzshare/cgi_qzshare_onekey";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, summary = shareOptions.summary;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        summary
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render30() {
    return this.generateComponent(this.networkURL);
  }
});
var SQZone_default = SQZone;

// node_modules/vue-socials/dist/esm/components/SReddit/SReddit.js
var SReddit = defineComponent({
  name: "SReddit",
  mixins: [BaseSocial_default("Reddit", void 0, void 0, void 0, false)],
  computed: {
    networkURL: function networkURL31() {
      var BASE_URL = "https://reddit.com/submit";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, text = shareOptions.text, selftext = shareOptions.selftext;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        text,
        selftext
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render31() {
    return this.generateComponent(this.networkURL);
  }
});
var SReddit_default = SReddit;

// node_modules/vue-socials/dist/esm/components/SRedditCount/SRedditCount.js
var SRedditCount = defineComponent({
  name: "SRedditCount",
  mixins: [BaseCount_default("Reddit")],
  methods: {
    handleRedditResponse: function handleRedditResponse(data) {
      var _data$data, _data$data$children, _data$data$children$;
      this.handleResult(data);
      this.handleCount(data === null || data === void 0 ? void 0 : (_data$data = data.data) === null || _data$data === void 0 ? void 0 : (_data$data$children = _data$data.children) === null || _data$data$children === void 0 ? void 0 : (_data$data$children$ = _data$data$children[0]) === null || _data$data$children$ === void 0 ? void 0 : _data$data$children$.data.score);
    }
  },
  mounted: function mounted9() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "https://www.reddit.com/api/info.json";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      limit: 1,
      url
    }));
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleRedditResponse(data);
      }
    }, "jsonp");
  }
});
var SRedditCount_default = SRedditCount;

// node_modules/vue-socials/dist/esm/components/SRenRen/SRenRen.js
var SRenRen = defineComponent({
  name: "SRenRen",
  mixins: [BaseSocial_default("RenRen")],
  computed: {
    networkURL: function networkURL32() {
      var BASE_URL = "http://widget.renren.com/dialog/share";
      var shareOptions = this.shareOptions;
      var resourceUrl = shareOptions.resourceUrl, srcUrl = shareOptions.srcUrl, title = shareOptions.title, description = shareOptions.description, pic = shareOptions.pic;
      var serialisedParams = getSerialisedParams_default({
        resourceUrl,
        srcUrl,
        title,
        description,
        pic
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render32() {
    return this.generateComponent(this.networkURL);
  }
});
var SRenRen_default = SRenRen;

// node_modules/vue-socials/dist/esm/components/SSkype/SSkype.js
var SSkype = defineComponent({
  name: "SSkype",
  mixins: [BaseSocial_default("Skype")],
  computed: {
    networkURL: function networkURL33() {
      var BASE_URL = "https://web.skype.com/share";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        url,
        text
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render33() {
    return this.generateComponent(this.networkURL);
  }
});
var SSkype_default = SSkype;

// node_modules/vue-socials/dist/esm/components/SSms/SSms.js
var SSms = defineComponent({
  name: "SSms",
  props: {
    /**
     * Share parameters for SMS
     */
    shareOptions: {
      type: Object,
      required: true
    }
  },
  computed: {
    networkURL: function networkURL34() {
      var BASE_URL = "sms:";
      var shareOptions = this.shareOptions;
      var number = shareOptions.number, body = shareOptions.body;
      var serialisedParams = getSerialisedParams_default({
        body
      });
      return "".concat(BASE_URL).concat(number).concat(serialisedParams);
    }
  },
  render: function render34() {
    var _this$$slots$default, _this$$slots;
    return h("a", {
      attrs: {
        href: this.networkURL
      }
    }, (_this$$slots$default = (_this$$slots = this.$slots).default) === null || _this$$slots$default === void 0 ? void 0 : _this$$slots$default.call(_this$$slots));
  }
});
var SSms_default = SSms;

// node_modules/vue-socials/dist/esm/components/STelegram/STelegram.js
var STelegram = defineComponent({
  name: "STelegram",
  mixins: [BaseSocial_default("Telegram")],
  computed: {
    networkURL: function networkURL35() {
      var BASE_URL = "https://t.me/share/url";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        url,
        text
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render35() {
    return this.generateComponent(this.networkURL);
  }
});
var STelegram_default = STelegram;

// node_modules/vue-socials/dist/esm/components/SThreema/SThreema.js
var SThreema = defineComponent({
  name: "SThreema",
  mixins: [BaseSocial_default("Threema")],
  computed: {
    networkURL: function networkURL36() {
      var BASE_URL = "threema://compose";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, id = shareOptions.id;
      var serialisedParams = getSerialisedParams_default({
        text: url,
        id
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render36() {
    return this.generateComponent(this.networkURL);
  }
});
var SThreema_default = SThreema;

// node_modules/vue-socials/dist/esm/components/STrello/STrello.js
var STrello = defineComponent({
  name: "STrello",
  mixins: [BaseSocial_default("Trello")],
  computed: {
    networkURL: function networkURL37() {
      var BASE_URL = "https://trello.com/add-card";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, name = shareOptions.name, description = shareOptions.description;
      var serialisedParams = getSerialisedParams_default({
        url,
        name,
        desc: description,
        mode: "popup"
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render37() {
    return this.generateComponent(this.networkURL);
  }
});
var STrello_default = STrello;

// node_modules/vue-socials/dist/esm/components/STumblr/STumblr.js
var STumblr = defineComponent({
  name: "STumblr",
  mixins: [BaseSocial_default("Tumblr", {
    width: 542,
    height: 644
  })],
  computed: {
    networkURL: function networkURL38() {
      var BASE_URL = "https://www.tumblr.com/widgets/share/tool";
      var shareOptions = this.shareOptions;
      var canonicalUrl = shareOptions.canonicalUrl, tags = shareOptions.tags, title = shareOptions.title, caption = shareOptions.caption, content = shareOptions.content, showVia = shareOptions.showVia;
      var serialisedParams = getSerialisedParams_default({
        canonicalUrl,
        title,
        caption,
        content,
        tags: getSeparatedList_default(tags),
        "show-via": showVia
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render38() {
    return this.generateComponent(this.networkURL);
  }
});
var STumblr_default = STumblr;

// node_modules/vue-socials/dist/esm/components/STumblrCount/STumblrCount.js
var STumblrCount = defineComponent({
  name: "STumblrCount",
  mixins: [BaseCount_default("Tumblr")],
  methods: {
    handleTumblrResponse: function handleTumblrResponse(data) {
      var _data$response;
      this.handleResult(data);
      this.handleCount((_data$response = data.response) === null || _data$response === void 0 ? void 0 : _data$response.note_count);
    }
  },
  mounted: function mounted10() {
    var _this = this;
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "http://api.tumblr.com/v2/share/stats";
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      url
    }));
    this.handleLoading(true);
    jsonp_default(finalURL, function(err, data) {
      _this.handleLoading(false);
      if (err) {
        _this.handleError(err);
      }
      if (data) {
        _this.handleTumblrResponse(data);
      }
    });
  }
});
var STumblrCount_default = STumblrCount;

// node_modules/vue-socials/dist/esm/components/STwitter/STwitter.js
var STwitter = defineComponent({
  name: "STwitter",
  mixins: [BaseSocial_default("Twitter", void 0, void 0, void 0, false)],
  computed: {
    networkURL: function networkURL39() {
      var BASE_URL = "https://twitter.com/intent/tweet";
      var shareOptions = this.shareOptions;
      var text = shareOptions.text, url = shareOptions.url, hashtags = shareOptions.hashtags, via = shareOptions.via;
      var serialisedParams = getSerialisedParams_default({
        text,
        url,
        hashtags: getSeparatedList_default(hashtags),
        via
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render39() {
    return this.generateComponent(this.networkURL);
  }
});
var STwitter_default = STwitter;

// node_modules/vue-socials/dist/esm/components/SViber/SViber.js
var SViber = defineComponent({
  name: "SViber",
  mixins: [BaseSocial_default("Viber")],
  computed: {
    networkURL: function networkURL40() {
      var BASE_URL = "viber://forward";
      var shareOptions = this.shareOptions;
      var text = shareOptions.text, url = shareOptions.url;
      var serialisedParams = getSerialisedParams_default({
        text: "".concat(text, " ").concat(url)
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render40() {
    return this.generateComponent(this.networkURL);
  }
});
var SViber_default = SViber;

// node_modules/vue-socials/dist/esm/components/SVkontakte/SVkontakte.js
var SVkontakte = defineComponent({
  name: "SVkontakte",
  mixins: [BaseSocial_default("Vkontakte", void 0, void 0, void 0, false)],
  computed: {
    networkURL: function networkURL41() {
      var BASE_URL = "https://vk.com/share.php";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, image = shareOptions.image, noParse = shareOptions.noParse, noVkLinks = shareOptions.noVkLinks;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        image,
        noparse: noParse,
        no_vk_links: Number(noVkLinks)
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render41() {
    return this.generateComponent(this.networkURL);
  }
});
var SVkontakte_default = SVkontakte;

// node_modules/vue-socials/dist/esm/components/SVkontakteCount/SVkontakteCount.js
var SVkontakteCount = defineComponent({
  mixins: [BaseCount_default("Vkontakte")],
  methods: {
    handleVKResponse: function handleVKResponse(index, count) {
      var _window$VK$callbacks;
      this.handleLoading(false);
      this.handleResult({
        index,
        count
      });
      this.handleCount(count);
      (_window$VK$callbacks = window.VK.callbacks) === null || _window$VK$callbacks === void 0 ? true : delete _window$VK$callbacks["cb".concat(index)];
    }
  },
  mounted: function mounted11() {
    var shareOptions = this.shareOptions;
    var url = shareOptions.url;
    var BASE_URL = "https://vk.com/share.php";
    if (!window.VK) {
      window.VK = {};
    }
    if (!window.VK.callbacks) {
      window.VK.callbacks = {};
    }
    window.VK.Share = {
      count: function(_count) {
        function count(_x, _x2) {
          return _count.apply(this, arguments);
        }
        count.toString = function() {
          return _count.toString();
        };
        return count;
      }(function(index2, count) {
        var _window$VK$callbacks2, _window$VK$callbacks3;
        return (_window$VK$callbacks2 = window.VK.callbacks) === null || _window$VK$callbacks2 === void 0 ? void 0 : (_window$VK$callbacks3 = _window$VK$callbacks2["cb".concat(index2)]) === null || _window$VK$callbacks3 === void 0 ? void 0 : _window$VK$callbacks3.call(_window$VK$callbacks2, index2, count);
      })
    };
    var index = Object.keys(window.VK.callbacks).length;
    var key = "cb".concat(index);
    window.VK.callbacks[key] = this.handleVKResponse;
    var finalURL = "".concat(BASE_URL).concat(getSerialisedParams_default({
      act: "count",
      index,
      url
    }));
    this.handleLoading(true);
    jsonp_default(finalURL);
  }
});
var SVkontakteCount_default = SVkontakteCount;

// node_modules/vue-socials/dist/esm/components/SWeibo/SWeibo.js
var SWeibo = defineComponent({
  name: "SWeibo",
  mixins: [BaseSocial_default("Weibo")],
  computed: {
    networkURL: function networkURL42() {
      var BASE_URL = "http://service.weibo.com/share/share.php";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, title = shareOptions.title, appkey = shareOptions.appkey, pic = shareOptions.pic;
      var serialisedParams = getSerialisedParams_default({
        url,
        title,
        appkey,
        pic
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render42() {
    return this.generateComponent(this.networkURL);
  }
});
var SWeibo_default = SWeibo;

// node_modules/vue-socials/dist/esm/components/SWhatsApp/SWhatsApp.js
var SWhatsApp = defineComponent({
  name: "SWhatsApp",
  mixins: [BaseSocial_default("WhatsApp")],
  computed: {
    networkURL: function networkURL43() {
      var BASE_URL = "https://api.whatsapp.com/send";
      var shareOptions = this.shareOptions;
      var number = shareOptions.number, text = shareOptions.text;
      var serialisedParams = getSerialisedParams_default({
        phone: number,
        text
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render43() {
    return this.generateComponent(this.networkURL);
  }
});
var SWhatsApp_default = SWhatsApp;

// node_modules/vue-socials/dist/esm/components/SWordPress/SWordPress.js
var SWordPress = defineComponent({
  name: "SWordPress",
  mixins: [BaseSocial_default("WordPress")],
  computed: {
    networkURL: function networkURL44() {
      var BASE_URL = "wp-admin/press-this.php";
      var shareOptions = this.shareOptions;
      var domain = shareOptions.domain, url = shareOptions.url, title = shareOptions.title, text = shareOptions.text, image = shareOptions.image;
      var serialisedParams = getSerialisedParams_default({
        u: url,
        t: title,
        s: text,
        i: image
      });
      return "".concat(domain, "/").concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render44() {
    return this.generateComponent(this.networkURL);
  }
});
var SWordPress_default = SWordPress;

// node_modules/vue-socials/dist/esm/components/SXing/SXing.js
var SXing = defineComponent({
  name: "SXing",
  mixins: [BaseSocial_default("Xing")],
  computed: {
    networkURL: function networkURL45() {
      var BASE_URL = "https://www.xing.com/spi/shares/new";
      var shareOptions = this.shareOptions;
      var url = shareOptions.url, followUrl = shareOptions.followUrl;
      var serialisedParams = getSerialisedParams_default({
        url,
        follow_url: followUrl
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render45() {
    return this.generateComponent(this.networkURL);
  }
});
var SXing_default = SXing;

// node_modules/vue-socials/dist/esm/components/SYahoo/SYahoo.js
var SYahoo = defineComponent({
  name: "SYahoo",
  mixins: [BaseSocial_default("Yahoo")],
  computed: {
    networkURL: function networkURL46() {
      var BASE_URL = "http://compose.mail.yahoo.com/";
      var shareOptions = this.shareOptions;
      var email = shareOptions.email, subject = shareOptions.subject, body = shareOptions.body;
      var serialisedParams = getSerialisedParams_default({
        to: email,
        subject,
        body
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render46() {
    return this.generateComponent(this.networkURL);
  }
});
var SYahoo_default = SYahoo;

// node_modules/vue-socials/dist/esm/components/SYammer/SYammer.js
var SYammer = defineComponent({
  name: "SYammer",
  mixins: [BaseSocial_default("Yammer", {
    width: 900,
    height: 600
  })],
  computed: {
    networkURL: function networkURL47() {
      var BASE_URL = "https://www.yammer.com/messages/new";
      var shareOptions = this.shareOptions;
      var text = shareOptions.text, url = shareOptions.url, groupId = shareOptions.groupId;
      var serialisedParams = getSerialisedParams_default({
        login: true,
        status: "".concat(text, " ").concat(url),
        group_id: groupId
      });
      return "".concat(BASE_URL).concat(serialisedParams);
    }
  },
  render: function render47() {
    return this.generateComponent(this.networkURL);
  }
});
var SYammer_default = SYammer;

// node_modules/vue-socials/dist/esm/vue-socials-esm.js
var install = function installVueSocials(app) {
  Object.entries(components_exports).forEach(function(item) {
    var componentName = item[0];
    var component = item[1];
    app.component(componentName, component);
  });
};
var vue_socials_esm_default = install;
export {
  SBlogger_default as SBlogger,
  SDevTo_default as SDevTo,
  SDiaspora_default as SDiaspora,
  SDouban_default as SDouban,
  SEmail_default as SEmail,
  SEvernote_default as SEvernote,
  SFacebook_default as SFacebook,
  SFacebookCount_default as SFacebookCount,
  SFacebookMessenger_default as SFacebookMessenger,
  SFacebookWorkplace_default as SFacebookWorkplace,
  SFlipBoard_default as SFlipBoard,
  SGithub_default as SGithub,
  SGithubCount_default as SGithubCount,
  SGithubGist_default as SGithubGist,
  SGithubGistCount_default as SGithubGistCount,
  SGithubRepo_default as SGithubRepo,
  SGithubRepoCount_default as SGithubRepoCount,
  SGmail_default as SGmail,
  SGoogleBookmarks_default as SGoogleBookmarks,
  SHackerNews_default as SHackerNews,
  SHatena_default as SHatena,
  SHatenaCount_default as SHatenaCount,
  SHouzz_default as SHouzz,
  SInstaPaper_default as SInstaPaper,
  SKakaoStory_default as SKakaoStory,
  SLine_default as SLine,
  SLineCount_default as SLineCount,
  SLinkedIn_default as SLinkedIn,
  SLiveJournal_default as SLiveJournal,
  SMailRu_default as SMailRu,
  SMastodon_default as SMastodon,
  SMicrosoftTeams_default as SMicrosoftTeams,
  SOdnoklassniki_default as SOdnoklassniki,
  SOdnoklassnikiCount_default as SOdnoklassnikiCount,
  SPinterest_default as SPinterest,
  SPinterestCount_default as SPinterestCount,
  SPocket_default as SPocket,
  SQZone_default as SQZone,
  SReddit_default as SReddit,
  SRedditCount_default as SRedditCount,
  SRenRen_default as SRenRen,
  SSkype_default as SSkype,
  SSms_default as SSms,
  STelegram_default as STelegram,
  SThreema_default as SThreema,
  STrello_default as STrello,
  STumblr_default as STumblr,
  STumblrCount_default as STumblrCount,
  STwitter_default as STwitter,
  SViber_default as SViber,
  SVkontakte_default as SVkontakte,
  SVkontakteCount_default as SVkontakteCount,
  SWeibo_default as SWeibo,
  SWhatsApp_default as SWhatsApp,
  SWordPress_default as SWordPress,
  SXing_default as SXing,
  SYahoo_default as SYahoo,
  SYammer_default as SYammer,
  vue_socials_esm_default as default
};
//# sourceMappingURL=vue-socials.js.map
